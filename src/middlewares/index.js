const errorHandlerMiddleware = require('./error-handler.middleware');
const asyncHandlerMiddleware = require('./async-handler.middleware');

module.exports = {
  errorHandlerMiddleware,
  asyncHandlerMiddleware
};
